const initialState = {
    lastMessages:[
        {id: 1, name:'Tasya'},
        {id: 2, name:'Anton'},
        {id: 3, name:'Alessya'}
    ]
}

export const navBarReducer = (state = initialState, action) => {
    return state;
}
